rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Rules for profile pictures
    match /profile-pictures/{userId}/{fileName} {
      // Anyone can read profile pictures
      allow read;
      
      // Only the authenticated user can write to their own profile picture path.
      allow write: if request.auth != null && request.auth.uid == userId;
    }

    // Rules for post images
    match /posts/{userId}/{fileName} {
      // Anyone can read post images
      allow read;

      // Only the authenticated user can create posts under their own user ID.
      allow write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
